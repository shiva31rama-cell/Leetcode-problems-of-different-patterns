# Tests

1. Insert `apple, app, ape`; prefix `ap` → `3`.
2. Delete `app`; prefix `ap` → `2`.
3. Delete missing word → `false`.
4. Duplicate insert `app` twice; delete once leaves prefix count unchanged for the remaining copy.
